
result = 1+2
print('计算1+2的结果\n计算结果为：',result)


ipadd = '{}.{}.{}.{} '.format(random.randint(1,255),
                              random.randint(1,255),
                              random.randint(1,255),
                              random.randint(1,255))
print(ipadd)
